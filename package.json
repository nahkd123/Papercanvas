{
  "name": "@nahkd123/papercanvas",
  "version": "1.0.0",
  "description": "Papercanvas",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "esbuild:build": "esbuild --bundle --platform=browser --global-name=app --asset-names=[name] --loader:.html=file --loader:.svg=file --loader:.png=dataurl --outdir=www src/index.ts",
    "esbuild:dev": "esbuild --bundle --platform=browser --global-name=app --asset-names=[name] --loader:.html=file --loader:.svg=file --loader:.png=dataurl --outdir=www src/index.ts --watch --servedir=www"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nahkd123/Papercanvas.git"
  },
  "keywords": [
    "html5",
    "drawing",
    "art"
  ],
  "author": "nahkd123",
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/nahkd123/Papercanvas/issues"
  },
  "homepage": "https://github.com/nahkd123/Papercanvas#readme",
  "devDependencies": {
    "esbuild": "^0.17.12",
    "typescript": "^5.0.2"
  }
}
